---
export let title = 'BJourneyGo';
export let description = 'Autenticación — BJourneyGo';
export let canonicalUrl = '';
export let image = '/social-card.svg';
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/png" href="/bJourneyGO4%20(1).png" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    {canonicalUrl ? <meta property="og:url" content={canonicalUrl} /> : null}
    <meta property="og:image" content={image} />
  </head>
  <body>
    <main>
      <slot />
    </main>

    <style is:global>
      :root{
        --bg: #0f0e10;
        --card: #1b1208;
        --muted: #efe6dc; /* lighter, higher-contrast muted text */
        --accent-1: #f97316; /* primary orange */
        --accent-2: #ffb86b; /* lighter orange */
        --glass: rgba(255,255,255,0.05);
      }
      *, *::before, *::after{box-sizing:border-box}
      html,body{height:100%;margin:0;width:100%}
      body{
        min-height:100svh;
        display:flex;
        align-items:center;
        justify-content:center;
        font-family: Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
        /* slightly darker overlay to increase card contrast */
        background-image: linear-gradient(rgba(17,8,5,0.72), rgba(17,8,5,0.72)), url('/login_background.jpg');
        background-color: #0f0e10;
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        color: #fffefb;
        line-height:1.5;
        overflow-x:hidden;
        touch-action: pan-y;
      }
      main{width:100%}
      .container{max-width:720px;width:100%;padding:1rem}
      .btn{display:inline-flex;align-items:center;justify-content:center;padding:.6rem .95rem;border-radius:10px;border:1px solid transparent;font-weight:600;text-decoration:none}
      .btn--primary{background:linear-gradient(90deg,var(--accent-1),var(--accent-2));color:#111;box-shadow:0 8px 20px rgba(249,115,22,0.16);border:none}
      .btn--ghost{background:transparent;color:var(--muted);border:1px solid rgba(255,255,255,0.06)}

      @media (max-width: 520px) {
        body {
          align-items: flex-start;
          padding-top: 0.75rem;
          padding-bottom: 1.5rem;
        }
      }
    </style>
  </body>
</html>
